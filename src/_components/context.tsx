import { createContext } from 'react';

export const FieldContext = createContext<any>({});
export const SchemaMarkupContext = createContext<any>({});
export const SchemaOptionsContext = createContext<any>({});
